# 2026-W31 주간 롤업

기간: 2026-07-27 ~ 2026-08-02

## 완료

- 최신 Unity automation 경쟁자와 Unity 공식 CLI/Skills 재조사
- Unity 6000.3.16f1 `unityctl-lab` 실험 프로젝트 생성
- `component get --full` hidden serialized state 누락 수정 및 live readback
- macOS interactive/headless Unity process inventory 구현 및 reload-aware `await-ready` 검증
- MCP Registry metadata/version/repository drift 수정
- v0.6.1 릴리스
- Claude Code/Codex 공용 `unityctl-workflows` 스킬 추가
- CLI/MCP NuGet package README 포함
- Linux `/proc` process inventory와 container probe 추가
- IPC listener publish/stop race 제거(master 후보)
- readiness probe를 bounded `ping` roundtrip으로 교체
- `project validate` 실패 의미론을 TestFailed/exit 1로 정정
- v0.6.3 IPC/validation hardening 릴리스 준비

## 주요 검증

- build warning 0 / error 0
- 전체 솔루션 962개 테스트 통과(PR 대상 939 + Integration 23)
- `Rigidbody.m_Constraints=80` live readback
- macOS Editor PID/process kind/worker 분류 확인
- v0.6.1 CLI/MCP package install + schema/tools smoke 통과
- 공용 agent harness validation 통과
- agent skill 정적 검증 + Claude Code/Codex 로컬 설치 smoke 통과
- CLI/MCP package README/nuspec smoke 통과
- Linux container `LinuxPlatformTests` 9개 통과(실제 `/proc` probe 포함)
- v0.6.2 CLI/MCP local install + 170-command schema/tools parity 통과
- Unity lab domain reload + UI close PID/lockfile cleanup 통과
- status 10회 expected pipe-close warning delta 0, await-ready 602ms
- public consumer onboarding smoke + validation failure/recovery live 검증
- final master 3-OS CI + published/local tool smoke 통과

## 다음

- Windows quit-hang #12/#13 reporter 재검증
- v0.6.3 release + Windows reporter 피드백 확인
