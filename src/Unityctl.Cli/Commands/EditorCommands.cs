namespace Unityctl.Cli.Commands;

public static class EditorCommands
{
    public static void List()
    {
        // Phase 1-A에서 UnityEditorDiscovery와 연결
        Console.WriteLine("Unity Editor discovery — coming in Phase 1-A");
    }
}
